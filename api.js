import { prepareFormData, sendRequest } from '../helpers/apiRequests';

// export const getStatuses = async () => {
//   const requestData = [{ name: 'action', value: 'GetSupportStatuses' }];
//   return sendRequest(await prepareFormData(requestData)).then(response => response);
// };

